export function qiblaBearing(lat,lon){const p=Math.PI/180,a=21.4225*Math.PI/180,b=39.8262*Math.PI/180,l=lat*Math.PI/180,o=lon*Math.PI/180;let x=Math.atan2(Math.sin(b-o),Math.cos(l)*Math.tan(a)-Math.sin(l)*Math.cos(b))*180/Math.PI;return (x+360)%360}
export async function geocode(city){const r=await fetch('https://geocoding-api.open-meteo.com/v1/search?name='+encodeURIComponent(city)+'&count=1&language=ar&format=json');const j=await r.json();return j.results?.[0]||null}
export async function prayer(city,country,method=3,school=0){const u=`https://api.aladhan.com/v1/timingsByCity?city=${encodeURIComponent(city)}&country=${encodeURIComponent(country)}&method=${method}&school=${school}`;const r=await fetch(u);if(!r.ok)throw Error('prayer');return r.json()}
export async function weather(lat,lon){const u=`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current=temperature_2m,relative_humidity_2m,weather_code,wind_speed_10m&timezone=auto`;const r=await fetch(u);return r.json()}
export function nextPrayer(t){const names=[['Fajr','الفجر'],['Sunrise','الشروق'],['Dhuhr','الظهر'],['Asr','العصر'],['Maghrib','المغرب'],['Isha','العشاء']];const now=new Date();let best=null;for(const [k,n] of names){if(!t[k])continue;let [h,m]=t[k].split(':').map(Number),d=new Date(now);d.setHours(h,m,0,0);if(d<=now)d.setDate(d.getDate()+1);if(!best||d<best.date)best={key:k,name:n,time:t[k],date:d}}return best}
